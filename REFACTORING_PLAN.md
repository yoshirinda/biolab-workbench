# 序列管理系统重构计划

## 目标
创建一个高质量、可维护、专业的 Web 序列管理系统

## 核心功能需求
1. ✅ 项目/文件夹管理（树形结构）
2. ✅ 序列导入/导出（FASTA, GenBank等）
3. ✅ 序列查看和编辑
4. ✅ 特征注释管理
5. ✅ 序列分析工具（翻译、反向互补、ORF查找等）
6. ✅ 文件拖拽上传

## 技术栈重构方案

### 后端（保持Flask）
```
Flask + Python 3.8+
├── 类型注解（Type Hints）
├── Pydantic（数据验证）
├── Flask-CORS（跨域）
├── 模块化架构
└── 统一错误处理
```

### 前端（简化方案 - 不引入复杂框架）
```
现代 Vanilla JavaScript
├── ES6+ 模块
├── 基于类的组件
├── 统一的工具库
├── 简单的状态管理
└── 响应式设计
```

**为什么不用React/Vue？**
- 项目规模中等，不需要重型框架
- 减少构建复杂度
- 保持简单易维护
- 性能更好

## 重构阶段

### 阶段1: 基础架构（今天）
- [x] 代码质量分析
- [ ] 创建统一工具模块
- [ ] 重构后端API结构
- [ ] 创建前端基础类库
- [ ] 统一错误处理

### 阶段2: 核心功能（明天）
- [ ] 重写项目管理模块
- [ ] 重写序列管理模块
- [ ] 重写文件上传模块
- [ ] 改进序列查看器

### 阶段3: 测试和优化（后天）
- [ ] 添加单元测试
- [ ] 性能优化
- [ ] 安全加固
- [ ] 文档完善

## 新的目录结构

### 后端
```
app/
├── __init__.py
├── api/                    # API层
│   ├── __init__.py
│   ├── projects.py        # 项目API
│   ├── sequences.py       # 序列API
│   ├── features.py        # 特征API
│   └── utils.py           # API工具
├── services/              # 业务逻辑层
│   ├── __init__.py
│   ├── project_service.py
│   ├── sequence_service.py
│   └── feature_service.py
├── models/                # 数据模型
│   ├── __init__.py
│   ├── project.py
│   ├── sequence.py
│   └── feature.py
├── utils/                 # 工具模块
│   ├── __init__.py
│   ├── validators.py      # 输入验证
│   ├── responses.py       # 统一响应
│   ├── errors.py          # 错误定义
│   └── decorators.py      # 装饰器
└── core/                  # 核心功能（保持现有）
    └── ...
```

### 前端
```
static/
├── js/
│   ├── lib/               # 基础库
│   │   ├── base-component.js      # 组件基类
│   │   ├── api-client.js          # API客户端
│   │   ├── event-bus.js           # 事件总线
│   │   ├── ui-helpers.js          # UI工具
│   │   └── validators.js          # 验证工具
│   ├── components/        # UI组件
│   │   ├── project-tree.js
│   │   ├── file-uploader.js
│   │   ├── sequence-viewer.js
│   │   └── sequence-editor.js
│   ├── services/          # 服务层
│   │   ├── project-service.js
│   │   ├── sequence-service.js
│   │   └── feature-service.js
│   └── app.js             # 应用入口
└── css/
    ├── base.css           # 基础样式
    ├── components.css     # 组件样式
    └── theme.css          # 主题变量
```

## 设计原则

### 1. SOLID原则
- **S**ingle Responsibility: 每个模块只做一件事
- **O**pen/Closed: 开放扩展，关闭修改
- **L**iskov Substitution: 子类可替换父类
- **I**nterface Segregation: 接口隔离
- **D**ependency Inversion: 依赖倒置

### 2. DRY（Don't Repeat Yourself）
- 消除所有重复代码
- 创建可复用的工具函数
- 使用继承和组合

### 3. KISS（Keep It Simple, Stupid）
- 简单直接的实现
- 避免过度设计
- 优先可读性

### 4. 关注点分离
- API层：只处理HTTP请求/响应
- Service层：业务逻辑
- Model层：数据定义
- Component层：UI渲染

## 代码规范

### Python
```python
# 使用类型注解
def create_project(name: str, parent_path: Optional[str] = None) -> Project:
    """创建新项目
    
    Args:
        name: 项目名称
        parent_path: 父路径（可选）
    
    Returns:
        Project: 创建的项目对象
    
    Raises:
        ValidationError: 验证失败
        ProjectExistsError: 项目已存在
    """
    pass

# 使用Pydantic进行验证
from pydantic import BaseModel, Field

class ProjectCreate(BaseModel):
    name: str = Field(..., min_length=1, max_length=255)
    parent_path: Optional[str] = None
    description: Optional[str] = None
```

### JavaScript
```javascript
// 使用ES6+类
class BaseComponent {
    constructor(containerId) {
        this.container = document.getElementById(containerId);
        this.state = {};
    }
    
    render() {
        // 子类实现
    }
    
    setState(newState) {
        this.state = { ...this.state, ...newState };
        this.render();
    }
}

// 使用模块
export class ProjectTree extends BaseComponent {
    // ...
}
```

## API设计规范

### RESTful API
```
GET    /api/projects              # 列出所有项目
POST   /api/projects              # 创建项目
GET    /api/projects/:id          # 获取项目详情
PUT    /api/projects/:id          # 更新项目
DELETE /api/projects/:id          # 删除项目

GET    /api/projects/:id/sequences      # 项目的序列列表
POST   /api/projects/:id/sequences      # 添加序列
GET    /api/sequences/:id                # 序列详情
PUT    /api/sequences/:id                # 更新序列
DELETE /api/sequences/:id              # 删除序列
```

### 统一响应格式
```json
// 成功响应
{
    "success": true,
    "data": { ... },
    "message": "操作成功"
}

// 错误响应
{
    "success": false,
    "error": {
        "code": "VALIDATION_ERROR",
        "message": "验证失败",
        "details": { ... }
    }
}
```

## 性能优化

1. **前端优化**
   - 虚拟滚动（大数据列表）
   - 防抖/节流
   - 懒加载
   - 缓存策略

2. **后端优化**
   - 数据库索引（如果使用DB）
   - 缓存（Redis）
   - 分页
   - 异步处理

## 安全措施

1. **输入验证** - 所有输入都验证
2. **CSRF保护** - Flask-WTF
3. **XSS防护** - 转义输出
4. **文件上传** - 大小限制、类型检查
5. **路径遍历** - 路径验证
6. **速率限制** - Flask-Limiter

## 测试策略

```
tests/
├── unit/                  # 单元测试
│   ├── test_services.py
│   ├── test_models.py
│   └── test_utils.py
├── integration/           # 集成测试
│   ├── test_api.py
│   └── test_workflows.py
└── e2e/                   # 端到端测试
    └── test_user_flows.py
```

## 下一步行动

### 今天（4-6小时）
1. ✅ 创建重构计划
2. 创建后端基础架构
3. 创建前端基础库
4. 重写项目管理API

### 明天（4-6小时）
1. 重写序列管理API
2. 重写前端组件
3. 集成测试

### 后天（2-3小时）
1. 添加测试
2. 文档完善
3. 部署准备

---

**总工作量估算: 10-15小时**

准备开始重构！🚀