/**
 * Legacy sequence bootstrap was retired after the React sequence app became
 * the default entry. Keep this file as a safe no-op because base.html still
 * loads /static/js/main.js globally.
 */
(function () {
    'use strict';
})();
